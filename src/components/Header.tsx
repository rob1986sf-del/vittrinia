import React from "react";

export default function Header() {
  return (
    <header style={{
      width: "100%",
      padding: "16px",
      background: "#111",
      color: "#fff",
      fontSize: "20px",
      fontWeight: "bold",
      textAlign: "center",
      borderBottom: "1px solid #333"
    }}>
      VITTRINIA — IA de Geração de Imagens
    </header>
  );
}
